<nav class="bg-gray-800 p-4">
    <div class="container mx-auto flex justify-between items-center">
        <a href="/category" class="text-white text-2xl font-bold hover:text-gray-400">Toska.</a>
        <div class="flex items-center">
            <% if (currentPage === 'movies') { %>
                <a href="/music" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:text-gray-400">Music</a>
            <% } else if (currentPage === 'music') { %>
                <a href="/movies" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:text-gray-400">Movies</a>
            <% } else { %>
                <a href="/movies" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:text-gray-400">Movies</a>
                <a href="/music" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:text-gray-400">Music</a>
            <% } %>
            <a href="/login" class="text-white px-3 py-2 rounded-md text-sm font-medium hover:text-gray-400">Log Out</a>
        </div>
        <form action="/search" method="GET" class="flex items-center ml-4">
            <input type="text" name="query" placeholder="Search..." class="px-3 py-2 rounded-md text-sm text-gray-700 focus:outline-none focus:ring-2 focus:ring-gray-500">
            <button type="submit" class="ml-2 bg-gray-700 text-white px-3 py-2 rounded-md text-sm font-medium hover:bg-gray-600">Search</button>
        </form>
    </div>
</nav>
